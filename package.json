{
  "name": "mediasoup-react",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix backend & npm run dev --prefix frontend",
    "install:all": "npm install --prefix backend && npm install --prefix frontend"
  }
}
